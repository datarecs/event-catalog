{
  "name": "@datarecs/event-catalog",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=26.0.0"
  },
  "scripts": {
    "dev": "eventcatalog dev",
    "build": "NODE_OPTIONS=--max-old-space-size=4096 eventcatalog build",
    "check:events": "node scripts/check-events-parity.mjs",
    "preview": "eventcatalog preview",
    "generate": "eventcatalog generate",
    "generate:events": "node scripts/generate-from-events.mjs"
  },
  "dependencies": {
    "@eventcatalog/core": "4.7.9"
  },
  "devDependencies": {}
}
